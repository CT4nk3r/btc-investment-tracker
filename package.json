{
  "name": "btc-investment-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev -H 0.0.0.0",
    "build": "next build",
    "start": "next start -H 0.0.0.0",
    "test": "vitest run tests --exclude tests/e2e/**",
    "test:e2e": "playwright test",
    "verify": "npm run test && npm run build && npm audit --omit=dev",
    "db:schema": "node scripts/apply-schema.mjs"
  },
  "dependencies": {
    "@clerk/nextjs": "^7.4.2",
    "@neondatabase/serverless": "^1.1.0",
    "lucide-react": "^0.555.0",
    "next": "^16.2.6",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "overrides": {
    "postcss": "^8.5.15"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vitest": "^4.1.7"
  }
}
